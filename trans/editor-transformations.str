module editor-transformations

imports
  libstratego-lib
  libstratego-gpp
  libstratego-aterm
  deriveTransformation
  
rules
  
  editor-detect-transformation :
    (selected, position, ast, path, project-path) -> ("optimization.bla", <length> trafo)
   	with
      trafo := <deriveTransformation> (ast,ast)